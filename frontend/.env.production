# Production build configuration (baked into the bundle at build time).
# AUTO-UPDATED by scripts/stack-watchdog.ps1 - trycloudflare URLs rotate on restart.
VITE_API_BASE=https://range-picking-stress-then.trycloudflare.com


# Production build configuration (baked into the bundle at build time).
# AUTO-UPDATED by scripts/stack-watchdog.ps1 - trycloudflare URLs rotate on restart.
# NOTE: rotation commits must NOT contain [skip ci] or Pages never deploys the new URL.
VITE_API_BASE=https://short-belong-induction-carroll.trycloudflare.com


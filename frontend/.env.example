# Frontend environment (Vite)
# Leave empty for local dev — Vite proxies /api to the backend at localhost:4000.
# For the deployed dashboard, set this to your tunnel hostname at BUILD time:
#   VITE_API_BASE=https://<your-tunnel-hostname>
VITE_API_BASE=
